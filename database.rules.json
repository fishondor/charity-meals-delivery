{
  "rules": {
    "deliveries": {
      ".read": true,
      ".write": "auth.uid != null",
      "$deliveryId": {
        ".write": "auth.uid === root.child('deliveries/' + $deliveryId + '/ownerId').val()",
        "carriers": {
          ".write": "auth.uid != null"
        }
      }
    },
    ".read": false,
    ".write": false
  }
}